const orderList = document.getElementsByClassName("lessons-learned");
const lessonsLearned = {
  lessons: 'Unix & Shell', 'Git', 'Git Hub', 
}
const insertLessons = () => {
  for (let i = 0; i < orderList.length; i += 1) {
    const li = document.creatElement("li");
    
  }
};
